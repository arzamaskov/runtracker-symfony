<script>
</script>

<section
	class="relative overflow-hidden"
	style="padding-top: var(--space-xl); padding-bottom: var(--space-xl);"
>
	<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
		<div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
			<!-- Left Column: Content -->
			<div class="text-left">
				<h1
					style="font-size: var(--h1-size); line-height: var(--h1-line); font-weight: 700; color: var(--color-text-primary); margin-bottom: var(--space-m);"
				>
					Раскрой свой <br class="hidden md:block" />
					<span style="color: var(--color-primary);">потенциал</span>
				</h1>

				<p
					style="font-size: var(--body-size); line-height: var(--body-line); color: var(--color-text-secondary); margin-bottom: var(--space-l); max-width: 480px;"
				>
					Ваш личный центр управления бегом. Анализируйте каждую деталь и достигайте новых высот.
				</p>

				<div class="flex flex-col sm:flex-row items-center gap-4">
					<a
						href="/register"
						class="inline-flex items-center justify-center transition-all shadow-sm hover:shadow-md"
						style="height: var(--btn-height); border-radius: var(--radius-control); padding: 0 32px; background: linear-gradient(90deg, var(--color-primary-gradient-from), var(--color-primary-gradient-to)); color: #FFFFFF; font-weight: 600; font-size: 16px;"
					>
						Регистрация
					</a>
					<a
						href="#features"
						class="inline-flex items-center justify-center transition-all hover:bg-slate-50"
						style="height: var(--btn-height); border-radius: var(--radius-control); padding: 0 32px; background: transparent; border: 1px solid var(--color-border); color: var(--color-text-primary); font-weight: 500; font-size: 16px;"
					>
						Узнать больше
					</a>
				</div>
			</div>

			<!-- Right Column: Image/Visual -->
			<div class="relative lg:h-auto flex items-center justify-center">
				<div
					class="relative w-full aspect-square max-w-md lg:max-w-full rounded-2xl overflow-hidden shadow-2xl"
					style="border-radius: var(--radius-large); box-shadow: var(--shadow-modal);"
				>
					<!-- Optional: Real image overlay if available -->
					<img src="/images/img2.png" alt="Runner" class="object-cover w-full h-full opacity-90" />
				</div>
			</div>
		</div>
	</div>
</section>
